# non ssl redirect to https://www. in case subdomain is not specified
server {
    listen 8080;
    server_name ~^(?<domain>[a-z0-9-]+)\.(?<tld>[a-z]+)(?<local>(|\.localhost))$;

    location / {
        return 301 https://www.$domain.$tld$local$request_uri;
    }
}

# non ssl redirect to https://
server {
    listen 8080;
    server_name ~^(?<subdomain>[a-z]+)\.(?<domain>[a-z0-9-]+)\.(?<tld>[a-z]+)(?<local>(|\.localhost))$;

    location / {
        return 301 https://$subdomain.$domain.$tld$local$request_uri;
    }
}

# ssl redirect to https://www. in case subdomain is not specified
server {
    listen 8443 ssl;
    server_name ~^(?<domain>[a-z0-9-]+)\.(?<tld>[a-z]+)(?<local>(|\.localhost))$;

    ssl_certificate /etc/customssl/live/fullchain.pem;
    ssl_certificate_key /etc/customssl/live/privkey.pem;

    location / {
        return 301 https://www.$domain.$tld$local$request_uri;
    }
}

# ssl default server
server {
    listen 8443 ssl default_server;
    server_name _;

    ssl_certificate /etc/customssl/live/fullchain.pem;
    ssl_certificate_key /etc/customssl/live/privkey.pem;

    location / {
        deny all;
    }
}

# server for domain pointed to this nginx server
server {
    listen 8443 ssl;
    server_name ~^(?<subdomain>[a-z]+)\.(?<domain>[a-z0-9-]+)\.(?<tld>[a-z]+)(?<local>(|\.localhost))$;

    ssl_certificate /etc/customssl/live/fullchain.pem;
    ssl_certificate_key /etc/customssl/live/privkey.pem;
    ssl_protocols TLSv1.3 TLSv1.2 TLSv1.1;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    root /var/www/html/public;
    error_page 500 502 503 504 /error.html;
    set $service_name extra-app;

    real_ip_header X-Forwarded-For;
    set_real_ip_from 192.168.0.0/16;

    location ~ /\. {
        try_files /dev/null @deny;
    }

    location ~ /\.py {
        try_files /dev/null @deny;
    }

    location ~ \.php {
        try_files /dev/null @deny;
    }

    location /nginxhealth {
        add_header Content-Type application/json;
        return 200 '{"app":"web-server","status":"ok","server":"$subdomain.$domain.$tld$local"}';
    }

    location ~ ^/(.*)\.(jpg|png|svg|css|js|txt|pdf|ico|woff|woff2|mp3|mp4|wav)$ {
        limit_req zone=assets_rate_limit burst=200 nodelay;
        expires 30d;
        try_files /$service_name$uri @external;
    }

    location /error.html {
        root /usr/share/nginx/html;
        internal;
    }

    location / {
        try_files /dev/null @application;
    }

    location @application {
        resolver ${NGINX_LOCATION_RESOLVER};
        include fastcgi_params;
        fastcgi_param HTTP_DATING_SITE $domain;
        fastcgi_param HTTP_SERVICE_NAME $service_name${NGINX_SERVICE_NAME_FQDN_ADDITION};
        fastcgi_param SCRIPT_FILENAME $document_root/index.php;
        fastcgi_pass $service_name${NGINX_SERVICE_NAME_FQDN_ADDITION}:9000;
    }

#    location /sockjs-node {
#        resolver 127.0.0.11;
#        proxy_set_header Upgrade $http_upgrade;
#        proxy_set_header Connection "upgrade";
#        proxy_pass http://$subdomain-$domain-$tld:3000;
#    }

#    location @dev_node_proxy {
#        resolver 127.0.0.11;
#        proxy_pass http://$subdomain-$domain-$tld:3000;
#    }

    location @deny {
        deny all;
    }

    location @external {
        deny all;
        #expires 30s;
        #return 301 https://somecdn_or_static_bucket/$server_name$request_uri;
    }
}
